05.14  10.14
缺少：


基本功能

注册	验证手机号是否注册，
	昵称敏感词过滤，（数据库查询功能）
	密码（正则表达式）
	电子邮件正则表达式
	验证码（canvas图片验证码）
	提交个人资料，更新数据库

登录	登录名，和密码验证
	cookie验证用户登录情况
	如果用户已登录，返回已登录信息
	成功跳转主页部分

主页部分
	三级导航
	轮播图
	点击logo可回到主页
	绝大部分使用数据渲染页面，
	购物车数量每次页面刷新自动获取对应用户的购物车商品类别的数量
	搜索功能（0518完成）	

列表页
	数据渲染
	实现排序功能，排序效果（0515完成）
	粗略版点击筛选功能，只有滋恩实现
	点击商品品实现跳转详情页

详情页
	渲染页面（0515完成）
	实现数据渲染
	点击数量增幅实现同步数据库
	放大镜功能
	回到顶部功能	
	回到顶部（0512完成）
	购物车侧栏固定（0513完成）
	购物推荐选项卡（数据同步未完成，数据改变未完成）
	点击选项卡跳到对应内容（锚点）
	验证用户是否登录，登录可继续操作购买选项
购物车
	页面刷新就验证用户登录状态
	若登录，则渲染数据库该用户的所有数据
	同步数量，点击按钮对应功能实现，
	单项，全选，单删，全删，同步勾选商品的数量和价格
	
	



	注册页面没有进行提示免登陆
	分页效果（0515完成）
	检索功能（主页未完成）
	
	购物车
	
	点击时添加动态（细节部分，个人添加完成）	

挑战：
	给用户传输头像（未完成）
	放大镜细节挑战（未完成）
	购买商品的路径暂时没有想到方法去完成	

	排序效果是否可以切换成列表格式（未完成）
	
	筛选功能叠加效果（未完成）
	

购物车添加思路：
		根据cookies的username去获取checkname的对应的用户id，
		判断是否登录
			如果登录了，就把商品对应的id和数量添加到该用户id相对应的数据库里面
					商品id，userid，商品价格price，数量num，和茶价hcprice
	
		点击加入购物车				点击立即购买		
		根据用户名查询用户个人信息		根据用户名查询用户个人信息
		查询个人信息有没有该shopid		查询个人信息有没有该shopid
判断语句，如果有就执行		增加数量		判断语句，如果有就执行		增加数量
	如果没有就执行		添加shopid和数量	如果没有就执行		添加shopid和数量
							
		同步执行的购物车的数量更新		同步执行的购物车的数量更新
							并且点击的该商品也要加入个人信息里面
							
	

	单选	购物车勾线的时候
		当我勾选其中一个时，该元素中input的值乘以单价，得到的结果会输出到结算
	多选	
		当勾选两个的时候，这两个input的标签的计算的结果会相加输出到结算
	核心	获取勾选的jq对象，我采用的是jq获取，获取input标签中checked的个数
		传入这两个数据进行遍历，遍历过程找到单价节点还有数量节点，累加并输出
	分歧点  取消勾选的时候，也需要重新计算，
	核心判断：当input中checked的个数等于0的时候；



















